---
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import Header from '../../components/Header.astro';
import FormattedDate from '../../components/FormattedDate.astro';

const allPosts = (await getCollection('blog')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
const weeklyPosts = allPosts.filter((p) => p.id.startsWith('weekly-'));
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title="Weekly Digest — payarch"
      description="Weekly briefings on agentic payments, protocol developments, and AI-native payment infrastructure."
      keywords={['weekly digest', 'agentic payments', 'payment news', 'AI payments']}
    />
    <style>
      main {
        width: 860px;
        max-width: 100%;
        box-sizing: border-box;
      }
      .page-header {
        margin-bottom: 3rem;
        border-bottom: 1px solid var(--border-color);
        padding-bottom: 2rem;
      }
      .page-header h1 {
        font-size: clamp(1.75rem, 4vw + 0.5rem, 2.5rem);
        margin: 0 0 0.5rem 0;
        line-height: 1.15;
      }
      .breadcrumbs {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        flex-wrap: wrap;
        margin-bottom: 1rem;
        font-family: 'Courier New', monospace;
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: var(--ink-500);
      }
      .breadcrumbs a {
        color: var(--ink-500);
        text-decoration: none;
      }
      .breadcrumbs a:hover { color: var(--ink-900) }
      .page-header p {
        color: var(--ink-500);
        font-size: clamp(1rem, 1vw + 0.9rem, 1.15rem);
        margin: 0;
        line-height: 1.5;
      }
      .weekly-list {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
        margin-bottom: 4rem;
      }
      .weekly-card {
        display: grid;
        grid-template-columns: 1fr auto;
        align-items: center;
        gap: 24px;
        padding: 2rem;
        border-radius: 16px;
        border: 1px solid var(--border-color);
        background: var(--paper-pure);
        color: inherit;
        text-decoration: none;
        transition: box-shadow 0.2s, transform 0.2s, border-color 0.2s;
        box-shadow: var(--shadow-card);
      }
      .weekly-card:hover {
        transform: translateY(-2px);
        box-shadow: var(--box-shadow-hover);
        border-color: var(--ink-300);
      }
      .issue-badge {
        font-family: 'Courier New', monospace;
        font-size: 0.72rem;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--ink-500);
        margin-bottom: 0.5rem;
      }
      .weekly-card h2 {
        font-size: 1.4rem;
        margin: 0 0 0.4rem 0;
        line-height: 1.25;
        color: var(--ink-900);
      }
      .weekly-card p {
        margin: 0;
        color: var(--ink-700);
        font-size: 1rem;
        line-height: 1.5;
      }
      .weekly-meta {
        display: flex;
        flex-direction: column;
        align-items: flex-end;
        gap: 6px;
        white-space: nowrap;
      }
      .weekly-date {
        font-family: 'Courier New', monospace;
        font-size: 0.8rem;
        color: var(--ink-500);
      }
      .weekly-arrow {
        font-size: 1.2rem;
        color: var(--ink-500);
        transition: transform .15s;
      }
      .weekly-card:hover .weekly-arrow { transform: translateX(3px) }
      @media (max-width: 720px) {
        .weekly-card { grid-template-columns: 1fr; gap: 12px; padding: 1.25rem }
        .weekly-meta { flex-direction: row; align-items: center }
      }
    </style>
  </head>
  <body>
    <Header />
    <main>
      <div class="page-header">
        <nav class="breadcrumbs" aria-label="Breadcrumb">
          <a href="/">Home</a>
          <span>/</span>
          <span aria-current="page">Weekly</span>
        </nav>
        <h1>Weekly Digest</h1>
        <p>Weekly briefings on agentic payments, protocol developments, and AI-native payment infrastructure. Published every week.</p>
      </div>

      <div class="weekly-list">
        {weeklyPosts.map((post, index) => (
          <a href={`/blog/${post.id}/`} class="weekly-card">
            <div>
              <div class="issue-badge">Issue #{String(weeklyPosts.length - index).padStart(2, '0')}</div>
              <h2>{post.data.title}</h2>
              {post.data.description && <p>{post.data.description}</p>}
            </div>
            <div class="weekly-meta">
              <span class="weekly-date"><FormattedDate date={post.data.pubDate} /></span>
              <span class="weekly-arrow">→</span>
            </div>
          </a>
        ))}
      </div>
    </main>
    <Footer />
  </body>
</html>
