---
const today = new Date();
---

<footer>
	&copy; {today.getFullYear()} payarch. All rights reserved.
</footer>
<style>
	footer {
		padding: 2em max(1em, env(safe-area-inset-right)) max(3em, calc(1.5em + env(safe-area-inset-bottom))) max(1em, env(safe-area-inset-left));
		background: var(--paper);
		color: var(--ink-500);
		text-align: center;
	}
</style>
